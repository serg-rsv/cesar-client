import { Box, Typography } from '@mui/material';

export const HomePage = () => {
  return (
    <Box>
      <Typography variant="h3" align="center" mt={4}>
        Cesar cipher app
      </Typography>
      <Typography variant="subtitle1" align="center" mt={4}>
        Unlock all the features by signing up or logging in.
      </Typography>
    </Box>
  );
};
